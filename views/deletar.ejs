<!DOCTYPE html>
<html lang="pt-br">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/details.css" />
    <title>Catalogo Lugares</title>
  </head>
  <body>

<body>
    <%- include('./components/header') %>
    <main>
        <div class="container">
            <span><% if(mensagem != ""){ mensagem } %></span>
            <form method="POST" action="/deletar/<%= filme.id %>">
                <div class="item">
                    <h2>Dejesa realmente deletar?</h2>
                    <span><%= lugares.nome %></span>
                </div>
                <div class="item">
                    <img src="<%= lugar.imagem %>" alt="lugar">
                </div>
                <div class="item">
                    <a href="/lugares/<%= lugar.id %>">Cancelar</a>
                    <Button type="submit">confirmar Exclusão</Button>
                </div>
                <!-- <input type="hidden" id="teste" name="teste"> -->
            </form>
        </div>
    </main>
   
        <%- include('./components/footer') %>
   
    <script src="./JS/script.js"></script> 
</body>
</html>